---
title: MCP 클라이언트 연결
description: MCP 클라이언트에서 outline-mcp를 실행하도록 설정합니다.
---

## 기본 설정

```json
{
  "mcpServers": {
    "outline": {
      "command": "npx",
      "args": ["-y", "@outline-mcp/mcp"],
      "env": {
        "OUTLINE_API_KEY": "your_api_key",
        "OUTLINE_BASE_URL": "https://app.getoutline.com",
        "OUTLINE_ALLOWED_ACTIONS": "read,write,delete"
      }
    }
  }
}
```

## 권장 적용 순서

1. `read`부터 시작
2. 도구 목록/문서 조회 검증
3. `read,write`로 확장
4. 삭제 정책이 명확해지면 `delete` 허용

## Self-hosted 엔드포인트

`OUTLINE_BASE_URL`만 변경하면 됩니다.

```bash
OUTLINE_BASE_URL=https://outline.your-company.internal
```
